version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout

      - run:
          name: Install packages
          command: npm install

      - run:
          name: Run tests
          command: npm test